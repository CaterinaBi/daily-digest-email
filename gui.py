from tkinter import *
from tkinter import tkk

class DailyDigestGUI:

    def __init__(self, root)
        pass #build the DailyDigestGUI

    """
    The DailyDigestGUI should enable the admin to...
    - configure the content sources to be included in the email
    - add/remove recepients
    - schedule daily time to send the email
    - configure sender credentials
    """

if __name__ == '__main__':
    root = Tk()
    app = DailyDigestGUI(root)
    root.mainloop()
